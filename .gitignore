Thumbs.db
.DS_Store
.dist
.tmp
.sass-cache
npm-debug.log
node_modules
package-lock.json
yarn.lock
netlify
# Build output. Netlify regenerates it via `npm run build` on every deploy,
# and the filenames are content-hashed, so committing it only churns diffs.
theme/
